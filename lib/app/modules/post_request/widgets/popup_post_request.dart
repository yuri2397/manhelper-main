import 'package:flutter/material.dart';

class PopupPostRequest extends StatelessWidget {
  const PopupPostRequest({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
