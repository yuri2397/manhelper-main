import 'package:flutter/material.dart';

class ThumbFile extends StatelessWidget {
  const ThumbFile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
